# Rust
/core/target/
Cargo.lock

# WASM build output (generated by web/scripts/build-wasm.mjs, run at build time)
/core/pkg/
/web/src/wasm/

# Node / web
/web/node_modules/
/web/dist/
/web/.vite/

# Data (large, fetched from Zenodo at runtime, never committed)
*.mat
*.wav
scripts/data/
scripts/reference/

# Editor / OS
.DS_Store
*.swp

# Claude Code project state
.claude/
